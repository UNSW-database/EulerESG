# Copy this file to .env in the repository root when local UI diagnostics are needed.
# true: show Backend System Status and the Next.js development indicator
# false: hide both controls (default)
NEXT_PUBLIC_SHOW_DEV_TOOLS=false
